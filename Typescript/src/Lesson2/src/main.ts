let myName: string = 'Hooman'
let yourName: string
yourName = 'John'

let meaningOfLife: number
meaningOfLife = 42

let isLoading: boolean;
isLoading = true;

let album: any
album = true;

//union type
let account: string | number;

//define a function

const sum = (a: number, b: number) => {
    return a + b
}

let postId: string | number
let isActive: number | boolean

//regular expression
let re: RegExp = /\w+/g
